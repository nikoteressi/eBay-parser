{
  "name": "eBay-parser",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "test": "DATABASE_PATH=/tmp/test-db.sqlite npm run db:push && DATABASE_PATH=/tmp/test-db.sqlite vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "drizzle-orm": "^0.45.2",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.5",
    "nuxt": "^4.4.2",
    "ulid": "^3.0.2",
    "vue": "^3.5.32",
    "vue-router": "^5.0.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node-cron": "^3.0.11",
    "@types/nodemailer": "^8.0.0",
    "@vitest/coverage-v8": "^4.1.4",
    "drizzle-kit": "^0.31.10",
    "vitest": "^4.1.4"
  },
  "overrides": {
    "esbuild": "0.28.0",
    "glob": "13.0.6"
  }
}
